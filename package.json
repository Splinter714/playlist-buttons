{
  "name": "playlist-buttons",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "shortcut:build": "node shortcut/build.mjs",
    "shortcut:inspect": "node shortcut/inspect.mjs",
    "shortcut:sign": "npm run shortcut:build && mkdir -p shortcut/dist/signed && shortcuts sign --mode anyone -i shortcut/dist/PlaylistButtons.shortcut -o shortcut/dist/signed/PlaylistButtons.shortcut"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
